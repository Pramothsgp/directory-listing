# Self-hosted font files
Place licensed Inter files here as `inter-regular.woff2`, `inter-semibold.woff2`, and `inter-bold.woff2`.
The static prototype uses system fallbacks when those binaries are absent.
